rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo permite leer y escribir en la colección 'informes' a usuarios autenticados.
    match /informes/{informeId} {
      allow read, write: if request.auth != null;
    }
  }
}
