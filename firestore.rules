rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite la lectura y escritura SÓLO si el usuario ha iniciado sesión.
      allow read, write: if request.auth != null;
    }
  }
}
