rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to the 'informes' collection only for authenticated users
    match /informes/{informeId} {
      allow read, write: if request.auth != null;
    }
  }
}
